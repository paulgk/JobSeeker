---
phase: 07-history-ui
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/score-card-display.tsx
  - src/components/action-list-display.tsx
  - src/components/keyword-badges-display.tsx
  - src/components/rewrite-diff-readonly.tsx
  - src/components/question-card-display.tsx
autonomous: true
requirements:
  - HIST-03
  - HIST-04
user_setup: []

must_haves:
  truths:
    - "ScoreCardDisplay renders an AnalysisResult's overallScore and components without mounting any SSE hook"
    - "ActionListDisplay renders AnalysisResult.actionItems without mounting any SSE hook"
    - "KeywordBadgesDisplay renders AnalysisResult.keywordGaps without mounting any SSE hook"
    - "RewriteDiffReadOnly renders a single accepted rewrite (sectionName + InlineDiff) with no Accept/Reject/Undo buttons and no callback props"
    - "QuestionCardDisplay renders a single interview question (number + question + category badge + expandable rationale + model answer) with no draft textarea, no critique button, and no streaming state"
    - "None of the five display components imports anything from src/app/api/* or from useAnalysis/useInterviewPrep hooks"
  artifacts:
    - path: "src/components/score-card-display.tsx"
      provides: "Re-export alias `ScoreCardDisplay` of ScoreCard"
      contains: "ScoreCard as ScoreCardDisplay"
    - path: "src/components/action-list-display.tsx"
      provides: "Re-export alias `ActionListDisplay` of ActionList"
      contains: "ActionList as ActionListDisplay"
    - path: "src/components/keyword-badges-display.tsx"
      provides: "Re-export alias `KeywordBadgesDisplay` of KeywordBadges"
      contains: "KeywordBadges as KeywordBadgesDisplay"
    - path: "src/components/rewrite-diff-readonly.tsx"
      provides: "Read-only rewrite diff card (accepted state only, no buttons)"
      contains: "export function RewriteDiffReadOnly"
    - path: "src/components/question-card-display.tsx"
      provides: "Read-only interview question card (no draft textarea, no critique)"
      contains: "export function QuestionCardDisplay"
  key_links:
    - from: "src/components/rewrite-diff-readonly.tsx"
      to: "InlineDiff from @/lib/diff and RewriteSection type"
      via: "named imports"
      pattern: "import .* from '@/lib/diff'"
    - from: "src/components/question-card-display.tsx"
      to: "InterviewPrepResult question shape from @/lib/schemas"
      via: "type import"
      pattern: "import type .* from '@/lib/schemas'"
---

<objective>
Create the five read-only display components needed by the /history/[id] detail page (Plan 07-04). Three are zero-logic aliases of existing stateless components (ScoreCard, ActionList, KeywordBadges). Two require trimming streaming/interactive code paths from their analogues (RewriteDiff → RewriteDiffReadOnly; QuestionCard → QuestionCardDisplay).

Purpose: Per the locked decision in STATE.md ("Read-only components for history detail — AnalysisPanel is wired to live SSE; build separate display-only components for saved data"), the detail page MUST NOT reuse AnalysisPanel or InterviewPrepPanel, which mount SSE hooks on render. This plan provides the safe display wrappers.

Output: Five new component files that compile cleanly, contain no `useEffect` SSE side-effects, and accept serialised data as props.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-history-ui/07-RESEARCH.md
@.planning/phases/07-history-ui/07-PATTERNS.md
@.planning/phases/07-history-ui/07-UI-SPEC.md
@src/components/score-card.tsx
@src/components/action-list.tsx
@src/components/keyword-badges.tsx
@src/components/rewrite-diff.tsx
@src/components/question-card.tsx
@src/lib/schemas.ts
@src/hooks/use-analysis.ts
@src/lib/diff.tsx
@CLAUDE.md
@AGENTS.md

<interfaces>
<!-- Extracted from src/components/*.tsx and src/lib/schemas.ts -->

ScoreCard (existing, src/components/score-card.tsx):
- No 'use client' directive, no hooks, no SSE
- Interface: { overallScore: number; components: AnalysisResult['components'] }
- Safe to re-export

ActionList (existing, src/components/action-list.tsx):
- No 'use client', no hooks
- Interface: { items: AnalysisResult['actionItems'] }
- Safe to re-export

KeywordBadges (existing, src/components/keyword-badges.tsx):
- No 'use client', no hooks
- Interface: { keywords: string[] }
- Safe to re-export

RewriteDiff (existing, src/components/rewrite-diff.tsx):
- 'use client' directive
- Interface: { rewrite: RewriteState; onAccept: () => void; onReject: () => void }
- RewriteState (from @/hooks/use-analysis) = { status: 'pending' | 'accepted' | 'rejected'; section: RewriteSection }
- RewriteSection = AnalysisResult['rewrites'][number] — shape: { sectionName: string; originalText: string; rewrittenText: string }
- MUST STRIP: 'use client', onAccept, onReject props, Button import, footer buttons block, the rejected/pending status branches
- KEEP: card chrome, sectionName header, "Accepted" Badge, InlineDiff render

QuestionCard (existing, src/components/question-card.tsx):
- 'use client' directive (kept — local toggle state)
- Interface (existing): { question; questionState; index; onToggle; onDraftChange; onSubmitCritique }
- MUST STRIP: questionState, onToggle, onDraftChange, onSubmitCritique props; Textarea section; Button (feedback) section; all critique display blocks (streaming/done/error)
- KEEP: header with question number + question text + category Badge + chevron toggle, expanded body with rationale and model answer
- ADD: local useState for `expanded` (replaces external onToggle wiring)

InterviewPrepResult question shape (from src/lib/schemas.ts):
- questions[number] = { question: string; category: 'behavioural' | 'technical' | 'situational' | 'role-specific'; rationale: string; modelAnswer: string }

InlineDiff (existing, src/lib/diff.tsx):
- Stateless render component; safe to import from any component
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create three thin re-export aliases for the already-stateless display components</name>
  <files>src/components/score-card-display.tsx, src/components/action-list-display.tsx, src/components/keyword-badges-display.tsx</files>
  <read_first>
    - src/components/score-card.tsx (confirm no 'use client', no hooks — verified)
    - src/components/action-list.tsx (confirm no 'use client', no hooks — verified)
    - src/components/keyword-badges.tsx (confirm no 'use client', no hooks — verified)
    - .planning/phases/07-history-ui/07-PATTERNS.md (re-export strategy explicitly preferred over copy)
  </read_first>
  <action>
    Create each of the three files as a single-line re-export aliasing the original component to a `*Display` name. This is the lower-maintenance path per 07-PATTERNS.md Open Question #3 — re-export wins.

    `src/components/score-card-display.tsx` contents (exact):
    `export { ScoreCard as ScoreCardDisplay } from '@/components/score-card'`

    `src/components/action-list-display.tsx` contents (exact):
    `export { ActionList as ActionListDisplay } from '@/components/action-list'`

    `src/components/keyword-badges-display.tsx` contents (exact):
    `export { KeywordBadges as KeywordBadgesDisplay } from '@/components/keyword-badges'`

    No extra exports, no comments, no type re-exports — consumers can import types directly from `@/lib/schemas` if needed.
  </action>
  <verify>
    <automated>test -f src/components/score-card-display.tsx && test -f src/components/action-list-display.tsx && test -f src/components/keyword-badges-display.tsx && grep -q "ScoreCard as ScoreCardDisplay" src/components/score-card-display.tsx && grep -q "ActionList as ActionListDisplay" src/components/action-list-display.tsx && grep -q "KeywordBadges as KeywordBadgesDisplay" src/components/keyword-badges-display.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The three files exist.
    - Each file contains exactly one `export { X as XDisplay } from '@/components/X'` line, with no other code.
    - `npx tsc --noEmit` exits 0.
    - Importing `import { ScoreCardDisplay } from '@/components/score-card-display'` from a Server Component does not trigger a "client component imported in server" build error (the underlying ScoreCard has no `'use client'` directive, so re-export remains server-compatible).
  </acceptance_criteria>
  <done>Three alias files exist; downstream detail page can import `ScoreCardDisplay`, `ActionListDisplay`, `KeywordBadgesDisplay` and reuse the stateless rendering logic without copy-paste drift.</done>
</task>

<task type="auto">
  <name>Task 2: Build RewriteDiffReadOnly and QuestionCardDisplay by stripping interactive/streaming logic from their analogues</name>
  <files>src/components/rewrite-diff-readonly.tsx, src/components/question-card-display.tsx</files>
  <read_first>
    - src/components/rewrite-diff.tsx (full file — identify lines 39-51 footer block to drop; the rejected/pending state branches to drop)
    - src/components/question-card.tsx (full file — identify the Textarea block lines 101-121, the feedback Button lines 124-131, and the three critique display blocks lines 133-166 to drop)
    - src/hooks/use-analysis.ts (RewriteSection type definition — RewriteSection = AnalysisResult['rewrites'][number])
    - src/lib/schemas.ts (InterviewPrepResult type for question shape)
    - src/lib/diff.tsx (InlineDiff signature)
    - .planning/phases/07-history-ui/07-PATTERNS.md (rewrite-diff-readonly.tsx and question-card-display.tsx sections — exact prop interfaces and render shape)
    - .planning/phases/07-history-ui/07-UI-SPEC.md (locked "Accepted" badge styling, typography contracts)
  </read_first>
  <action>
    Create `src/components/rewrite-diff-readonly.tsx`:
    - NO `'use client'` directive (component is pure render — server-renderable).
    - Imports: `import { InlineDiff } from '@/lib/diff'`, `import { Badge } from '@/components/ui/badge'`, and a local type alias for the section shape — since `RewriteSection` lives in `@/hooks/use-analysis.ts` which is a `'use client'` module, IMPORT THE SECTION TYPE INDIRECTLY by deriving it locally from the schema: `import type { AnalysisResult } from '@/lib/schemas'`, then `type RewriteSection = AnalysisResult['rewrites'][number]`. This avoids accidentally pulling client-only code into a Server Component import chain.
    - Interface: `interface RewriteDiffReadOnlyProps { section: RewriteSection }` — pass only the section, not the wrapping RewriteState (callers filter for `status === 'accepted'` before rendering).
    - Export `function RewriteDiffReadOnly({ section }: RewriteDiffReadOnlyProps)`.
    - Render (copy from rewrite-diff.tsx lines 18-37, with all status branches removed):
      - Outer: `<div className="rounded-xl ring-1 ring-border overflow-hidden">`.
      - Header: `<div className="px-5 py-4 flex items-center justify-between gap-3 border-b border-border bg-card"><span className="text-sm font-semibold text-foreground">{section.sectionName}</span><Badge className="bg-accent text-foreground border-0 text-xs">Accepted</Badge></div>`.
      - Body: `<div className="px-5 py-4 bg-background"><InlineDiff original={section.originalText} rewritten={section.rewrittenText} /></div>`.
    - Do NOT render any footer/button area. Do NOT support pending or rejected states — caller filters.

    Create `src/components/question-card-display.tsx`:
    - `'use client'` directive at top (needed for local `expanded` useState).
    - Imports: `import { useState } from 'react'`, `import { ChevronDown, ChevronUp } from 'lucide-react'`, `import type { InterviewPrepResult } from '@/lib/schemas'`, `import { Badge } from '@/components/ui/badge'`.
    - Copy categoryLabel and categoryVariant maps verbatim from question-card.tsx lines 19-34.
    - Interface: `interface QuestionCardDisplayProps { question: InterviewPrepResult['questions'][number]; index: number }`.
    - Export `function QuestionCardDisplay({ question, index }: QuestionCardDisplayProps)`.
    - Local state: `const [expanded, setExpanded] = useState(false)`.
    - Render (copy from question-card.tsx lines 53-99 — header button + expanded rationale + model answer ONLY):
      - Outer: `<div className="rounded-xl ring-1 ring-border bg-card text-sm text-card-foreground overflow-hidden">`.
      - Header button: `<button type="button" onClick={() => setExpanded(v => !v)} className="w-full flex items-start gap-3 px-4 py-3 text-left hover:bg-secondary/50 transition-colors" aria-expanded={expanded}>` containing number span, question span, and the Badge + ChevronUp/Down icon block as in the original.
      - Expanded body (only when `expanded`): `<div className="px-4 pb-4 space-y-4 border-t border-border">` containing the rationale block (lines 84-89 of question-card.tsx) and the model answer block (lines 92-99 of question-card.tsx).
    - DO NOT include: the draft Textarea label/input block (lines 101-121), the feedback Button (lines 124-131), nor any of the three critique display blocks (lines 133-166). Do NOT import Textarea, Button, or QuestionState.
  </action>
  <verify>
    <automated>test -f src/components/rewrite-diff-readonly.tsx && test -f src/components/question-card-display.tsx && grep -q "export function RewriteDiffReadOnly" src/components/rewrite-diff-readonly.tsx && grep -q "export function QuestionCardDisplay" src/components/question-card-display.tsx && grep -vq "Textarea\\|onAccept\\|onReject\\|onSubmitCritique\\|critiquePhase\\|critiqueText" src/components/question-card-display.tsx && grep -vq "Button\\|onAccept\\|onReject" src/components/rewrite-diff-readonly.tsx && grep -q "'use client'" src/components/question-card-display.tsx && grep -L "'use client'" src/components/rewrite-diff-readonly.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/rewrite-diff-readonly.tsx` exists, does NOT contain `'use client'`, does NOT contain `Button`, `onAccept`, `onReject`, or `Undo` (verified by `grep -E "'use client'|Button|onAccept|onReject|Undo" src/components/rewrite-diff-readonly.tsx` returns empty).
    - `RewriteDiffReadOnly` is exported and accepts a single `section` prop.
    - Source contains the literal `Accepted` (badge label) and renders `InlineDiff`.
    - File `src/components/question-card-display.tsx` exists, contains `'use client'`, contains `export function QuestionCardDisplay`, and does NOT contain any of these substrings: `Textarea`, `onDraftChange`, `onSubmitCritique`, `onToggle`, `critiquePhase`, `critiqueText`, `feedbackLabel`, `QuestionState` (verified by `grep -E "Textarea|onDraftChange|onSubmitCritique|onToggle|critiquePhase|critiqueText|feedbackLabel|QuestionState" src/components/question-card-display.tsx` returns empty).
    - `QuestionCardDisplay` uses local `useState` for `expanded` (verified by `grep -q "useState(false)" src/components/question-card-display.tsx`).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Both stripped-down display components compile and render with only the fields they need; neither imports any SSE hook, callback prop, or interactive helper.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server page → display components | Components render persisted data fetched server-side; no untrusted client input flows here |
| display components → DOM | JSONB strings (rewrittenText, modelAnswer, rationale, sectionName) are rendered as React text — React escapes by default |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-12 | Tampering | XSS via JSONB content rendered in display components | mitigate | All rendered fields (sectionName, originalText, rewrittenText, question, rationale, modelAnswer) are inserted via React JSX text interpolation `{value}` — React HTML-escapes by default. No `dangerouslySetInnerHTML` introduced. InlineDiff (existing) also uses safe text rendering. |
| T-07-13 | Information Disclosure | Accidental SSE call from display component | mitigate | Display components contain NO `useEffect` SSE fetches, NO imports from `@/hooks/use-analysis` or `@/hooks/use-interview-prep`, and NO imports from `@/components/analysis-panel` or `@/components/interview-prep-panel`. Verified by acceptance grep "Textarea|critiquePhase|onSubmitCritique" returning empty for question-card-display, and "Button|onAccept|onReject" returning empty for rewrite-diff-readonly. |
| T-07-14 | Tampering | Stale RewriteState semantics | mitigate | RewriteDiffReadOnly accepts only `section` (the underlying RewriteSection shape), not RewriteState. Caller is responsible for filtering to accepted rewrites only — pushes the state semantics out of the display component, eliminating any chance of rendering a rejected rewrite as accepted. |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | Zero package installs in this plan — pure source file creation. Package Legitimacy Audit in 07-RESEARCH.md confirms no new runtime deps for Phase 7 beyond shadcn-generated source (handled in Plan 07-01). |
</threat_model>

<verification>
- All five new files compile under `npx tsc --noEmit`.
- `grep -rn "useEffect\|useAnalysis\|useInterviewPrep\|fetch(" src/components/score-card-display.tsx src/components/action-list-display.tsx src/components/keyword-badges-display.tsx src/components/rewrite-diff-readonly.tsx src/components/question-card-display.tsx` returns empty — no side-effects, no streaming hooks.
- Importing `ScoreCardDisplay`, `ActionListDisplay`, `KeywordBadgesDisplay`, `RewriteDiffReadOnly` into a Server Component does not produce a build error (none carry `'use client'`).
- `QuestionCardDisplay` is `'use client'` (justified — local toggle state); importing it into a Server Component remains valid (Next.js auto-detects the boundary).
</verification>

<success_criteria>
- Detail page (Plan 07-04) can render saved analysis and interview Q+A using these five components without triggering any SSE call or live API hit.
- HIST-03 and HIST-04 read-only requirements are unblocked.
</success_criteria>

<output>
Create `.planning/phases/07-history-ui/07-03-SUMMARY.md` when done — list the five new files, the three that are pure aliases, and confirm via grep transcript that no interactive/streaming substrings leaked into the two trimmed components.
</output>
