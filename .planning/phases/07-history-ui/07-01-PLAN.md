---
phase: 07-history-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/dal.ts
  - src/components/ui/select.tsx
  - src/components/auth-header.tsx
  - src/components/status-select.tsx
  - src/app/history/page.tsx
autonomous: true
requirements:
  - HIST-01
  - HIST-02
user_setup: []

must_haves:
  truths:
    - "Authenticated user visiting /history sees a list of their saved applications with company, job title, match score, status badge, and date"
    - "Unauthenticated user visiting /history is redirected to /sign-in (already enforced by proxy.ts)"
    - "The history list query does NOT select resumeText, jdText, analysisData, or interviewData JSONB columns"
    - "Applications appear newest-first (createdAt desc)"
    - "Empty state copy 'No applications saved yet' renders when the user has zero saved applications"
    - "AuthHeader shows JobSeeker wordmark, History link, user email, and a working Sign out button on /history"
    - "User can change status on a list row via a Select dropdown; the badge label updates immediately (optimistic)"
  artifacts:
    - path: "src/components/ui/select.tsx"
      provides: "shadcn Select primitive (installed via npx shadcn add)"
      contains: "export { Select"
    - path: "src/components/auth-header.tsx"
      provides: "Shared client header with sign-out"
      contains: "export function AuthHeader"
    - path: "src/components/status-select.tsx"
      provides: "Optimistic status-update Select bound to /api/applications/[id]/status"
      contains: "export function StatusSelect"
    - path: "src/app/history/page.tsx"
      provides: "Server Component listing the signed-in user's applications"
      contains: "export default async function HistoryPage"
    - path: "src/lib/dal.ts"
      provides: "getApplications() ordered by createdAt desc"
      contains: "desc(applications.createdAt)"
  key_links:
    - from: "src/app/history/page.tsx"
      to: "verifySession() and getApplications(userId) in src/lib/dal.ts"
      via: "direct server-side import"
      pattern: "import .* from '@/lib/dal'"
    - from: "src/app/history/page.tsx"
      to: "src/components/auth-header.tsx and src/components/status-select.tsx"
      via: "React component import"
      pattern: "import .* from '@/components/(auth-header|status-select)'"
    - from: "src/components/status-select.tsx"
      to: "PATCH /api/applications/[id]/status (created in Plan 07-02)"
      via: "fetch with method PATCH"
      pattern: "fetch\\(`/api/applications/\\$\\{[^}]+\\}/status`"
    - from: "src/components/auth-header.tsx"
      to: "signOut from @/lib/auth-client"
      via: "client-side call with router.push redirect"
      pattern: "signOut\\("
---

<objective>
Build the /history list page (HIST-01) plus the two shared client components needed to render rows and the page chrome: AuthHeader (sticky header with sign-out) and StatusSelect (optimistic status dropdown that will be reused on the detail page in Plan 07-04). Also install the shadcn Select primitive and switch getApplications() to newest-first ordering.

Purpose: Deliver the metadata-only history list with the locked dark-first b2fA design and prepare reusable header/status components consumed by the detail page in Wave 2.

Output: A working `/history` route that loads server-side via verifySession() + getApplications(), renders application rows with status select + match-score color + date, and a populated empty state when the user has no records.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-history-ui/07-RESEARCH.md
@.planning/phases/07-history-ui/07-PATTERNS.md
@.planning/phases/07-history-ui/07-UI-SPEC.md
@src/lib/dal.ts
@src/lib/db/schema.ts
@src/lib/auth-client.ts
@src/app/page.tsx
@src/app/sign-in/page.tsx
@src/components/score-card.tsx
@src/components/ui/badge.tsx
@CLAUDE.md
@AGENTS.md

<interfaces>
<!-- Extracted from src/lib/dal.ts and src/lib/db/schema.ts so the executor does not need to re-grep. -->

From src/lib/dal.ts (existing, modify orderBy in this plan):
- verifySession(): cache wrapped — returns { isAuth: true, userId: string, user: User }; redirects to /sign-in if no session
- getApplications(userId): returns array of metadata rows { id, userId, jobTitle, company, status, matchScore, createdAt, updatedAt } — NO JSONB columns selected

From src/lib/db/schema.ts:
- ApplicationStatus = 'saved' | 'applied' | 'interviewing' | 'offer' | 'rejected'  (lowercase enum values — DB values)
- Application row shape (metadata): { id: string (uuid), userId: string, jobTitle: string, company: string, status: ApplicationStatus, matchScore: number | null, createdAt: Date, updatedAt: Date }

From src/lib/auth-client.ts:
- signOut, signIn, signUp, useSession exported from authClient
- signOut takes an options object; better-auth supports fetchOptions.onSuccess callback for post-signout redirect

From @/components/ui/badge (existing):
- Badge variants: default, secondary, outline, destructive

Status badge variant mapping (locked by UI-SPEC):
- saved → variant="default"
- applied → variant="secondary"
- interviewing → variant="secondary"
- offer → variant="default"
- rejected → variant="destructive"

UI label mapping (display capitalization):
- saved → "Saved", applied → "Applied", interviewing → "Interviewing", offer → "Offer", rejected → "Rejected"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn select, switch getApplications() to newest-first, add AuthHeader + StatusSelect client components</name>
  <files>src/components/ui/select.tsx, src/lib/dal.ts, src/components/auth-header.tsx, src/components/status-select.tsx</files>
  <read_first>
    - src/lib/dal.ts (current getApplications uses .orderBy(applications.createdAt) ascending — must swap to desc)
    - src/lib/db/schema.ts (ApplicationStatus enum values — lowercase: 'saved' | 'applied' | 'interviewing' | 'offer' | 'rejected')
    - src/lib/auth-client.ts (signOut export)
    - src/app/sign-in/page.tsx (loading/error state pattern lines 14-19, 151-157 — copy shape for StatusSelect)
    - src/app/page.tsx (existing card style + header shape)
    - .planning/phases/07-history-ui/07-UI-SPEC.md (AuthHeader layout contract + StatusSelect width 140px + status label/variant maps)
    - .planning/phases/07-history-ui/07-PATTERNS.md (auth-header.tsx and status-select.tsx full pattern sections)
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/page.md (Next.js 16 page semantics; AGENTS.md mandates reading this before writing Next.js code)
  </read_first>
  <action>
    Run `npx shadcn@4.8.0 add select` from the repo root to install `src/components/ui/select.tsx` (the shadcn nova-style Select primitive). Do not pass `--yes` for any cleanup flags; accept defaults.

    Modify `src/lib/dal.ts`: add `desc` to the drizzle-orm named import (currently `import { eq, and } from 'drizzle-orm'`), and change the `getApplications` ORDER BY from `.orderBy(applications.createdAt)` to `.orderBy(desc(applications.createdAt))`. No other changes to dal.ts.

    Create `src/components/auth-header.tsx` as a client component (`'use client'` directive). Imports: `Link` from `next/link`, `useRouter` from `next/navigation`, `signOut` from `@/lib/auth-client`, `Button` from `@/components/ui/button`. Export `AuthHeader({ userEmail }: { userEmail: string })`. Layout (per UI-SPEC): `<header className="sticky top-0 z-10 h-16 bg-card border-b border-border flex items-center px-6">` containing (a) JobSeeker wordmark as `<Link href="/" className="text-xl font-semibold tracking-tight text-foreground mr-auto">JobSeeker</Link>`, (b) a `<nav>` with `History` link (`href="/history"`, `text-sm text-muted-foreground hover:text-foreground`), the email span (`text-xs text-muted-foreground`), and a `<Button variant="ghost" size="sm" onClick={handleSignOut}>Sign out</Button>`. `handleSignOut` is async: `await signOut({ fetchOptions: { onSuccess: () => router.push('/sign-in') } })`. Copy strings must match UI-SPEC Copywriting Contract exactly: "History", "Sign out".

    Create `src/components/status-select.tsx` as a client component (`'use client'`). Imports: `useState` from `react`, `Select`, `SelectContent`, `SelectItem`, `SelectTrigger`, `SelectValue` from `@/components/ui/select`, and `type { ApplicationStatus }` from `@/lib/db/schema`. Define `STATUS_LABELS: Record<ApplicationStatus, string>` with values `{ saved: 'Saved', applied: 'Applied', interviewing: 'Interviewing', offer: 'Offer', rejected: 'Rejected' }`. Export `StatusSelect({ id, initialStatus }: { id: string; initialStatus: ApplicationStatus })`. State: `status` (initialStatus), `error` (string | null), `saving` (boolean). `handleChange(newStatus)`: capture `prev = status`, set status optimistically, clear error, set saving true, call `fetch(`/api/applications/${id}/status`, { method: 'PATCH', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ status: newStatus }) })`, set saving false, if `!res.ok` set status back to `prev` and set error to "Couldn't save status. Try again." (exact copy from UI-SPEC). Render the shadcn `Select` with `value={status}`, `onValueChange={(v) => handleChange(v as ApplicationStatus)}`, `disabled={saving}`. `SelectTrigger` className `w-[140px]` (locked width from UI-SPEC). `SelectContent` lists all 5 statuses via `Object.entries(STATUS_LABELS).map(([value, label]) => <SelectItem key={value} value={value}>{label}</SelectItem>)`. Below the Select, conditionally render `{error && <p className="text-sm text-destructive mt-1">{error}</p>}`.

    Do NOT add any console.log, comments above 'Component:', or extra abstractions. Do NOT touch proxy.ts (the /history guard already exists per RESEARCH.md and PATTERNS.md — verify-only, no edit).
  </action>
  <verify>
    <automated>test -f src/components/ui/select.tsx && test -f src/components/auth-header.tsx && test -f src/components/status-select.tsx && grep -q "desc(applications.createdAt)" src/lib/dal.ts && grep -q "desc" src/lib/dal.ts | head -1 && grep -q "export function AuthHeader" src/components/auth-header.tsx && grep -q "export function StatusSelect" src/components/status-select.tsx && grep -q "Couldn't save status. Try again." src/components/status-select.tsx && grep -q "w-\\[140px\\]" src/components/status-select.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/ui/select.tsx` exists and exports `Select`, `SelectContent`, `SelectItem`, `SelectTrigger`, `SelectValue` (verified via `grep -E "export \\{ Select" src/components/ui/select.tsx`).
    - `src/lib/dal.ts` imports `desc` from `drizzle-orm` and `getApplications` uses `.orderBy(desc(applications.createdAt))` (verified via `grep -c "desc(applications.createdAt)" src/lib/dal.ts` returns 1).
    - `src/components/auth-header.tsx` starts with `'use client'`, contains `export function AuthHeader`, calls `signOut(` with `fetchOptions.onSuccess` callback, and includes the exact strings `"History"`, `"Sign out"`, and `"JobSeeker"`.
    - `src/components/status-select.tsx` starts with `'use client'`, contains `export function StatusSelect`, contains the literal `STATUS_LABELS` map with the five lowercase keys mapped to capitalised labels, fetches `PATCH /api/applications/${id}/status` with `Content-Type: application/json` body `{ status: newStatus }`, reverts `status` to `prev` on `!res.ok`, and shows the exact error string `"Couldn't save status. Try again."`.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Select primitive installed, DAL ordering flipped to newest-first, AuthHeader and StatusSelect components compile and follow UI-SPEC exactly (sticky header, 140px select width, locked copy strings).</done>
</task>

<task type="auto">
  <name>Task 2: Build /history server-component list page with empty state, row layout, score color, and date formatting</name>
  <files>src/app/history/page.tsx</files>
  <read_first>
    - src/lib/dal.ts (verifySession, getApplications signatures — note verifySession returns { userId, user }; verifySession redirects internally so no try/catch needed)
    - src/lib/db/schema.ts (ApplicationStatus enum + Application row shape)
    - src/app/page.tsx (existing layout className patterns: rounded-2xl ring-1 ring-border bg-card)
    - src/components/auth-header.tsx (Task 1 output — props { userEmail })
    - src/components/status-select.tsx (Task 1 output — props { id, initialStatus })
    - src/components/ui/badge.tsx (Badge variant prop)
    - src/components/score-card.tsx (lines 9, 18-20 — isStrong + inline style color pattern for score ≥ 70)
    - .planning/phases/07-history-ui/07-UI-SPEC.md (List page layout contract + Copywriting Contract: "My Applications", "No applications saved yet", "Run your first analysis and it will appear here.", "Analyse a job → ")
    - .planning/phases/07-history-ui/07-PATTERNS.md (history/page.tsx pattern section + card class + score color pattern)
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/page.md (Server Component pages are async by default in Next.js 16; per AGENTS.md)
  </read_first>
  <action>
    Create `src/app/history/page.tsx` as a Next.js 16 Server Component (no `'use client'` directive; default async function export).

    Imports: `verifySession`, `getApplications` from `@/lib/dal`; `Link` from `next/link`; `AuthHeader` from `@/components/auth-header`; `StatusSelect` from `@/components/status-select`; `Badge` from `@/components/ui/badge`; `type { ApplicationStatus }` from `@/lib/db/schema`.

    Top of file: define two module-level constants exactly as locked in UI-SPEC.
    - `STATUS_VARIANT: Record<ApplicationStatus, 'default' | 'secondary' | 'outline' | 'destructive'>` = `{ saved: 'default', applied: 'secondary', interviewing: 'secondary', offer: 'default', rejected: 'destructive' }`.
    - `STATUS_LABELS: Record<ApplicationStatus, string>` = `{ saved: 'Saved', applied: 'Applied', interviewing: 'Interviewing', offer: 'Offer', rejected: 'Rejected' }`.
    - `dateFormatter = new Intl.DateTimeFormat('en-US', { month: 'long', day: 'numeric', year: 'numeric' })` — produces strings like "May 26, 2026".

    `export default async function HistoryPage()`:
    1. `const { userId, user } = await verifySession()` — verifySession redirects internally, no try/catch.
    2. `const apps = await getApplications(userId)` — returns metadata-only rows, already ordered newest-first by Task 1.
    3. Return JSX with this structure:
       - React fragment wrapping `<AuthHeader userEmail={user.email} />` and `<main className="max-w-4xl mx-auto px-6 py-8">`.
       - `<h1 className="text-2xl font-semibold tracking-tight text-foreground mb-8">My Applications</h1>`.
       - If `apps.length === 0`: render an empty state with `<div className="rounded-2xl ring-1 ring-border bg-card p-10 text-center"><h2 className="text-base font-semibold text-foreground mb-2">No applications saved yet</h2><p className="text-sm text-muted-foreground mb-4">Run your first analysis and it will appear here.</p><Link href="/" className="text-sm text-foreground hover:underline">Analyse a job → </Link></div>`. Copy strings are LOCKED per UI-SPEC — do not paraphrase, do not drop the trailing space-arrow in the CTA.
       - Otherwise render `<div className="space-y-3">` containing one row per app.
    4. Row markup per application (NOT a separate component file — inline JSX inside `apps.map(...)`):
       - Outer: `<Link href={`/history/${app.id}`} key={app.id} className="block rounded-2xl ring-1 ring-border bg-card px-6 py-4 hover:bg-card/80 transition-colors">`.
       - Inside the Link, flex layout: `<div className="flex items-center gap-4">`.
       - Left column (`flex-1 min-w-0`): two stacked spans — company name `<span className="text-base font-semibold text-foreground truncate block">{app.company}</span>` and job title `<span className="text-sm text-muted-foreground truncate block">{app.jobTitle}</span>`.
       - Score: `const isStrong = app.matchScore != null && app.matchScore >= 70;` then `<span className="text-sm font-semibold tabular-nums" style={{ color: isStrong ? 'oklch(0.72 0.12 68)' : undefined }}>{app.matchScore != null ? `${app.matchScore}%` : '—'}</span>`. (Inline style required — dynamic Tailwind class would be purged at build per STATE.md.)
       - Status badge: `<Badge variant={STATUS_VARIANT[app.status]}>{STATUS_LABELS[app.status]}</Badge>`.
       - Status select: wrap in `<div onClick={(e) => e.preventDefault()}>` to stop the surrounding Link from navigating when the user opens the select. Render `<StatusSelect id={app.id} initialStatus={app.status} />` inside that wrapper. Note: this requires the wrapper div be a client component — since StatusSelect is already a client component and the wrapper just stops propagation, you must use `onClick` only if the parent boundary supports it. Solution: place the wrapper element inside StatusSelect itself? NO — keep the wrapper in this page and accept that the parent Link's default navigation is blocked by the wrapper. Implement the wrapper as a simple inline `<div className="flex-none" onClick={(e) => { e.preventDefault(); e.stopPropagation(); }}>`. Since `onClick` requires a client component boundary, instead set `<div className="flex-none" data-no-row-link="">` and wrap the StatusSelect in a `'use client'` wrapper inside `src/components/status-select.tsx`? Concretely: in `status-select.tsx`, wrap the returned `<Select>` in a `<div onClick={(e) => e.stopPropagation()}>` so the propagation guard lives inside the already-client component. (Update Task 1's `status-select.tsx` if you have already implemented it without this wrapper — but since both tasks are in the same plan, prefer adding the wrapper in Task 1 from the start.)
       - Date: `<span className="text-xs text-muted-foreground tabular-nums">{dateFormatter.format(new Date(app.createdAt))}</span>`.

    Important: Row navigation uses Next `<Link>` so it works without JavaScript. The status select must NOT navigate — its propagation guard is the wrapper div inside StatusSelect itself (move that wrapper into status-select.tsx during Task 1 so this page does not need to handle propagation).

    Do NOT add a loading skeleton (server-rendered, no loading state). Do NOT select JSONB columns (already enforced by getApplications). Do NOT introduce a new "rowClickable" component — inline JSX matches the project simplicity convention.
  </action>
  <verify>
    <automated>test -f src/app/history/page.tsx && grep -q "export default async function HistoryPage" src/app/history/page.tsx && grep -q "verifySession" src/app/history/page.tsx && grep -q "getApplications" src/app/history/page.tsx && grep -q "My Applications" src/app/history/page.tsx && grep -q "No applications saved yet" src/app/history/page.tsx && grep -q "max-w-4xl" src/app/history/page.tsx && grep -q "Intl.DateTimeFormat" src/app/history/page.tsx && grep -q "oklch(0.72 0.12 68)" src/app/history/page.tsx && grep -vq "analysisData\\|interviewData\\|resumeText\\|jdText" src/app/history/page.tsx && npx tsc --noEmit && npm run lint --silent 2>&1 | grep -v "warning" | grep -q "error" && echo "lint errors" || echo "lint clean"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/app/history/page.tsx` exists, contains `export default async function HistoryPage`, awaits `verifySession()` and `getApplications(userId)`, and renders `<AuthHeader userEmail={user.email} />`.
    - The literal strings `"My Applications"`, `"No applications saved yet"`, `"Run your first analysis and it will appear here."`, and `"Analyse a job → "` all appear in the file (`grep -c` returns ≥ 1 for each).
    - The page references `STATUS_VARIANT` and `STATUS_LABELS` records with all five keys `saved`, `applied`, `interviewing`, `offer`, `rejected` (verified by `grep -E "saved:|applied:|interviewing:|offer:|rejected:" src/app/history/page.tsx | wc -l` returns ≥ 10 — two maps × 5 keys).
    - The score color uses the inline style literal `oklch(0.72 0.12 68)` (no dynamic Tailwind class).
    - The page does NOT contain the substrings `analysisData`, `interviewData`, `resumeText`, or `jdText` (verified by `grep -E "analysisData|interviewData|resumeText|jdText" src/app/history/page.tsx` returns empty).
    - The page imports `AuthHeader` from `@/components/auth-header` and `StatusSelect` from `@/components/status-select`.
    - `StatusSelect`'s root element stops click propagation (verified by `grep -q "stopPropagation" src/components/status-select.tsx`).
    - `npx tsc --noEmit` exits 0.
    - Behavioural (manual confirmation by running dev server): visiting `/history` as a signed-in user renders the list; visiting unauthenticated redirects to `/sign-in`; visiting as a signed-in user with zero rows renders the empty state with the locked CTA link.
  </acceptance_criteria>
  <done>/history renders newest-first, metadata-only, with locked dark-theme card styling; empty state has locked copy + CTA; row click navigates to /history/[id] (route created in Plan 07-04); status select within a row does not trigger row navigation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /history (server page) | Untrusted user session cookie crosses into server-rendered page |
| browser → PATCH /api/applications/[id]/status | Untrusted status value + ID cross into route handler (handler is created in Plan 07-02; StatusSelect is the producer here) |
| client (StatusSelect) → server (PATCH route) | Untrusted optimistic state must not be confused with persisted state on revert |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | /history server page | mitigate | verifySession() called first — redirects to /sign-in if no session. getApplications(userId) WHERE userId = ? — IDOR guard built into DAL. Confirmed in dal.ts lines 27-42. |
| T-07-02 | Information Disclosure | /history server page (JSONB columns) | mitigate | getApplications selects only metadata columns (id, userId, jobTitle, company, status, matchScore, createdAt, updatedAt). resumeText/jdText/analysisData/interviewData are never selected — confirmed in dal.ts. Page must not import dal.ts's getApplicationById here. Verified by acceptance grep. |
| T-07-03 | Tampering | StatusSelect optimistic UI | mitigate | On `!res.ok` the component reverts `status` to the previous value and surfaces an inline error so the persisted DB state is the source of truth. Disabling Select while `saving=true` prevents concurrent stale PATCHes. |
| T-07-04 | Spoofing | AuthHeader sign-out flow | mitigate | signOut() routes through better-auth which clears the session cookie server-side; on success router.push('/sign-in') prevents stale UI showing protected nav. |
| T-07-05 | Repudiation | Status change without audit | accept | v1 single-user tool, no audit trail in v1.1 scope. updatedAt timestamp on the row is the only evidence — accepted per project simplicity principle. |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | Only install: `npx shadcn@4.8.0 add select`. shadcn pinned to 4.8.0 (in package.json). Generated component is local source — no runtime npm dep added beyond @radix-ui/react-select (Radix primitives already used elsewhere). Package Legitimacy Audit in 07-RESEARCH.md confirms disposition: Approved. No [ASSUMED]/[SUS] packages → no human checkpoint required. |
</threat_model>

<verification>
- `/history` returns 200 for authenticated user with rows; returns 200 with empty-state JSX for authenticated user with zero rows; returns 307 redirect to `/sign-in` for unauthenticated request (proxy.ts guard).
- `getApplications` SELECT statement (visible in DB query log if needed) lists only metadata columns — no SELECT of `resumeText`, `jdText`, `analysisData`, or `interviewData`.
- TypeScript compiles cleanly: `npx tsc --noEmit` exits 0.
- Lint: `npm run lint` reports no errors in the new files.
</verification>

<success_criteria>
- Visiting `/history` while signed-in shows a list of applications with company, job title, score (or "—" when null), status badge, status select, and date — newest first.
- Empty state shows when zero apps exist, with locked copy "No applications saved yet" + "Run your first analysis and it will appear here." + "Analyse a job → " link to /.
- Visiting `/history` while signed-out redirects to `/sign-in` (already enforced by proxy.ts — no change required).
- AuthHeader sticky on scroll, sign-out clears session and routes to /sign-in.
- Status select changes are optimistic; reverting on PATCH failure shows the locked error string "Couldn't save status. Try again." (PATCH endpoint provided by Plan 07-02 running in the same wave).
</success_criteria>

<output>
Create `.planning/phases/07-history-ui/07-01-SUMMARY.md` when done — list files added/modified, any deviations, and confirmation that all acceptance criteria are met.
</output>
