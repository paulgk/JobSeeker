---
phase: 07-history-ui
plan: 04
type: execute
wave: 2
depends_on:
  - 07-01
  - 07-02
  - 07-03
files_modified:
  - src/app/api/applications/[id]/prefill/route.ts
  - src/app/history/[id]/page.tsx
  - src/components/resume-panel.tsx
  - src/components/jd-panel.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - HIST-03
  - HIST-04
  - HIST-05
user_setup: []

must_haves:
  truths:
    - "Authenticated user can open /history/[id] for an application they own and see the company/job title heading, status select, Re-run button, and a Tabs control"
    - "/history/[id] for an unauthenticated user redirects to /sign-in (proxy.ts guard)"
    - "/history/[id] for an ID that does not exist or is owned by another user returns Next.js 404 (notFound())"
    - "Analysis tab renders ScoreCardDisplay, ActionListDisplay, KeywordBadgesDisplay, and RewriteDiffReadOnly for each accepted rewrite — purely from the persisted analysisData JSONB"
    - "Interview Prep tab renders QuestionCardDisplay for every question when interviewData is non-null; otherwise renders the empty-state copy from UI-SPEC"
    - "Clicking Re-run navigates the browser to /?applicationId={id}"
    - "Visiting /?applicationId={id} pre-populates the resume textarea and JD textarea from the saved application's resumeText and jdText"
    - "Visiting /?applicationId={id} also surfaces those values up to the page's resumeText/jdText state so the Analyse button enables"
    - "GET /api/applications/[id]/prefill returns 200 with { resumeText, jdText } for the owner, 401 unauthenticated, 404 for non-owners"
  artifacts:
    - path: "src/app/api/applications/[id]/prefill/route.ts"
      provides: "GET route returning { resumeText, jdText } scoped to the signed-in user"
      contains: "export async function GET"
    - path: "src/app/history/[id]/page.tsx"
      provides: "Server Component detail page with Tabs + read-only panels"
      contains: "export default async function DetailPage"
    - path: "src/components/resume-panel.tsx"
      provides: "Accepts optional initialValue and reports it via onReady"
      contains: "initialValue"
    - path: "src/components/jd-panel.tsx"
      provides: "Accepts optional initialValue and reports it via onReady"
      contains: "initialValue"
    - path: "src/app/page.tsx"
      provides: "Reads searchParams.applicationId via use(), prefetches via /api/applications/[id]/prefill, threads initialValue into panels"
      contains: "use(searchParams)"
  key_links:
    - from: "src/app/history/[id]/page.tsx"
      to: "verifySession() + getApplicationById() in src/lib/dal.ts"
      via: "server-side import"
      pattern: "import .* from '@/lib/dal'"
    - from: "src/app/history/[id]/page.tsx"
      to: "AuthHeader, StatusSelect, five display components"
      via: "React component imports"
      pattern: "import .* from '@/components/(auth-header|status-select|score-card-display|action-list-display|keyword-badges-display|rewrite-diff-readonly|question-card-display)'"
    - from: "src/app/page.tsx"
      to: "GET /api/applications/[id]/prefill"
      via: "fetch in useEffect when applicationId is present"
      pattern: "fetch\\(`/api/applications/\\$\\{[^}]+\\}/prefill`"
    - from: "src/app/api/applications/[id]/prefill/route.ts"
      to: "verifySession() + getApplicationById() in src/lib/dal.ts"
      via: "server-side import"
      pattern: "import .* from '@/lib/dal'"
---

<objective>
Close HIST-03, HIST-04, and HIST-05 by building the detail page (with Tabs for Analysis and Interview Prep), the prefill GET route, and the home-page re-run plumbing. The detail page reuses AuthHeader and StatusSelect (Plan 07-01) and the five display components (Plan 07-03). The Re-run flow follows the STATE.md locked decision: navigate to `/?applicationId=ID` → home page client component reads `searchParams` via React's `use()` → useEffect fetches prefill → panels receive `initialValue` prop.

Purpose: Final wave — wires every Wave 1 artifact together and delivers the user-visible flows for the three remaining requirements.

Output: A working `/history/[id]` route that renders saved analysis + interview prep read-only and a working Re-run button that returns the user to the home page with the saved resume + JD pre-populated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-history-ui/07-RESEARCH.md
@.planning/phases/07-history-ui/07-PATTERNS.md
@.planning/phases/07-history-ui/07-UI-SPEC.md
@.planning/phases/07-history-ui/07-01-PLAN.md
@.planning/phases/07-history-ui/07-02-PLAN.md
@.planning/phases/07-history-ui/07-03-PLAN.md
@src/lib/dal.ts
@src/lib/db/schema.ts
@src/lib/schemas.ts
@src/app/page.tsx
@src/components/resume-panel.tsx
@src/components/jd-panel.tsx
@src/components/ui/tabs.tsx
@src/components/ui/button.tsx
@src/components/auth-header.tsx
@src/components/status-select.tsx
@src/app/api/analyse/route.ts
@src/app/api/fetch-jd/route.ts
@CLAUDE.md
@AGENTS.md

<interfaces>
<!-- Extracted directly from source -->

From src/lib/dal.ts:
- verifySession() — returns { isAuth, userId, user }; in route handlers throws on no session, in server pages redirects to /sign-in
- getApplicationById(userId, id) — returns Application | null (full row with JSONB); IDOR guard built-in

From src/lib/db/schema.ts:
- Application = full row: { id, userId, jobTitle, company, status, matchScore, resumeText, jdText, analysisData: AnalysisResult | null, interviewData: InterviewPrepResult | null, createdAt, updatedAt }

From src/lib/schemas.ts:
- AnalysisResult = { overallScore: number, components: [...], actionItems: [...], keywordGaps: string[], rewrites: [...] }
- InterviewPrepResult = { questions: [{ question, category, rationale, modelAnswer }] }

From Plan 07-01:
- AuthHeader({ userEmail }) — 'use client' header with sign-out
- StatusSelect({ id, initialStatus }) — 'use client' optimistic dropdown; stops propagation (wrapper inside the component)

From Plan 07-03:
- ScoreCardDisplay({ overallScore, components }) — re-export
- ActionListDisplay({ items }) — re-export
- KeywordBadgesDisplay({ keywords }) — re-export
- RewriteDiffReadOnly({ section }) — accepts RewriteSection only; caller filters by status
- QuestionCardDisplay({ question, index }) — 'use client'; local expand toggle

From src/components/resume-panel.tsx (current — needs initialValue prop added):
- Props today: { onReady?: (text: string) => void; locked?: boolean }
- Internal state: extractedText, pasteText, tab ('upload' | 'paste'), fileName, isLoading, error
- onReady is invoked when the user confirms upload-parse success or paste-confirm

From src/components/jd-panel.tsx (current — needs initialValue prop added):
- Props today: { onReady?: (text: string) => void; locked?: boolean }
- Internal state: pasteText, urlInput, extractedText, tab ('paste' | 'url'), isLoading, error

From src/app/page.tsx:
- 'use client' page with useState for resumeText, jdText, analysing, showInterviewPrep, savedApplicationId
- Currently has NO searchParams parameter

Next.js 16 page conventions (per node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/page.md):
- searchParams in Client Components MUST be read with React's use(): `const { applicationId } = use(searchParams)` — typed as `Promise<{ ... }>`
- params in Server Components: `const { id } = await params` — typed as `Promise<{ ... }>`

Status badge variant + label maps (locked in UI-SPEC) — already defined in /history list page (Plan 07-01); for consistency repeat the same two maps at top of /history/[id]/page.tsx (do not import from list page — pages are sibling routes, not a shared component module).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GET /api/applications/[id]/prefill route + add initialValue prop to ResumePanel and JobDescriptionPanel</name>
  <files>src/app/api/applications/[id]/prefill/route.ts, src/components/resume-panel.tsx, src/components/jd-panel.tsx</files>
  <read_first>
    - src/app/api/analyse/route.ts lines 47-65 (auth + JSON parse guard pattern — only auth needed here)
    - src/app/api/fetch-jd/route.ts (thin JSON-returning GET shape; though it is POST, the response idiom is the same)
    - src/lib/dal.ts (getApplicationById signature + null-on-not-found semantics)
    - src/components/resume-panel.tsx (full file — must add initialValue without disrupting tab switching or extractedText state)
    - src/components/jd-panel.tsx (full file — same)
    - src/components/text-preview.tsx if present (used by both panels to render extractedText)
    - .planning/phases/07-history-ui/07-PATTERNS.md (prefill route section + Open Question #1 — initialValue prop addition)
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/route.md (Route Handler params Promise unwrap)
  </read_first>
  <action>
    Step A — Create `src/app/api/applications/[id]/prefill/route.ts`:
    - Imports: `import { NextRequest } from 'next/server'`, `import { verifySession, getApplicationById } from '@/lib/dal'`.
    - Export `async function GET(_request: NextRequest, { params }: { params: Promise<{ id: string }> })`.
    - Auth guard — copy verbatim pattern from `src/app/api/analyse/route.ts` lines 49-54: `let userId: string; try { const session = await verifySession(); userId = session.userId } catch { return new Response(null, { status: 401 }) }`.
    - `const { id } = await params`.
    - `const app = await getApplicationById(userId, id); if (!app) return new Response(null, { status: 404 })`.
    - `return Response.json({ resumeText: app.resumeText, jdText: app.jdText })` — do NOT include any analysisData, interviewData, jobTitle, company, status, matchScore, or timestamp fields. Re-run only needs the source texts.
    - Do not export `runtime`, `dynamic`, `maxDuration` — small synchronous read.

    Step B — Modify `src/components/resume-panel.tsx`:
    - Extend the props interface to `interface ResumePanelProps { onReady?: (text: string) => void; locked?: boolean; initialValue?: string }`.
    - Destructure `initialValue` in the function parameters: `export function ResumePanel({ onReady, locked, initialValue }: ResumePanelProps)`.
    - Inside the function body, after the existing `useState` calls, add a `useEffect` that fires only when `initialValue` changes to a non-empty value:
      ```
      useEffect(() => {
        if (!initialValue) return
        // Switch to paste tab so the textarea is visible
        setTab('paste')
        setPasteText(initialValue)
        setExtractedText(initialValue)
        setError(null)
        onReady?.(initialValue)
      }, [initialValue])
      ```
    - Add `useEffect` to the existing `react` import (currently `import { useState, useRef } from 'react'` — change to `import { useState, useRef, useEffect } from 'react'`).
    - Do NOT touch the existing upload/parse flow, the drag-and-drop handlers, or the paste-confirm button. The new useEffect is additive.

    Step C — Modify `src/components/jd-panel.tsx`:
    - Mirror Step B: add `initialValue?: string` to `JobDescriptionPanelProps`, destructure it, add `useEffect` import to the existing `import { useState } from 'react'` line (→ `import { useState, useEffect } from 'react'`), and add the same effect block:
      ```
      useEffect(() => {
        if (!initialValue) return
        setTab('paste')
        setPasteText(initialValue)
        setExtractedText(initialValue)
        setError(null)
        onReady?.(initialValue)
      }, [initialValue])
      ```

    Note: the effect intentionally depends on `initialValue` only. ESLint may warn that `onReady` and `setTab`/`setPasteText`/`setExtractedText`/`setError` are missing from the dep array. Disable that specific warning inline for this hook with `// eslint-disable-next-line react-hooks/exhaustive-deps` on the line above the dep array — re-firing on `onReady` identity changes would loop the parent state setter. (Setters from useState are stable per React docs; safe to omit.)
  </action>
  <verify>
    <automated>test -f 'src/app/api/applications/[id]/prefill/route.ts' && grep -q "export async function GET" 'src/app/api/applications/[id]/prefill/route.ts' && grep -q "verifySession" 'src/app/api/applications/[id]/prefill/route.ts' && grep -q "getApplicationById" 'src/app/api/applications/[id]/prefill/route.ts' && grep -q "status: 401" 'src/app/api/applications/[id]/prefill/route.ts' && grep -q "status: 404" 'src/app/api/applications/[id]/prefill/route.ts' && grep -q "resumeText: app.resumeText" 'src/app/api/applications/[id]/prefill/route.ts' && grep -q "initialValue" src/components/resume-panel.tsx && grep -q "initialValue" src/components/jd-panel.tsx && grep -q "useEffect" src/components/resume-panel.tsx && grep -q "useEffect" src/components/jd-panel.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `src/app/api/applications/[id]/prefill/route.ts` exists with `export async function GET`, awaits `params`, uses `verifySession` + `getApplicationById`, and returns one of three branches: 401 (no session), 404 (no row), 200 JSON `{ resumeText, jdText }`.
    - The route does NOT include `analysisData`, `interviewData`, `jobTitle`, `company`, `status`, `matchScore`, `createdAt`, or `updatedAt` in the response body (verified by `grep -E "analysisData|interviewData|jobTitle|company|status|matchScore|createdAt|updatedAt" 'src/app/api/applications/[id]/prefill/route.ts'` returns empty).
    - `src/components/resume-panel.tsx` and `src/components/jd-panel.tsx` both contain `initialValue` in the props interface and a `useEffect` keyed on `[initialValue]`.
    - `npx tsc --noEmit` exits 0.
    - Behavioural:
      - `curl http://localhost:3000/api/applications/{owned-uuid}/prefill` with valid session cookie returns 200 with JSON body containing `resumeText` and `jdText` keys only.
      - Without cookie returns 401 empty.
      - With a random UUID not in DB returns 404 empty.
  </acceptance_criteria>
  <done>Prefill route returns the minimum payload needed for re-run; both input panels accept an optional initialValue and surface it through their existing onReady contract so the parent page's resumeText/jdText state populates without changing the existing onReady wiring.</done>
</task>

<task type="auto">
  <name>Task 2: Build /history/[id] detail page with Tabs, read-only panels, status select, Re-run button, and back link</name>
  <files>src/app/history/[id]/page.tsx</files>
  <read_first>
    - src/lib/dal.ts (getApplicationById return shape)
    - src/lib/db/schema.ts (Application + ApplicationStatus)
    - src/lib/schemas.ts (AnalysisResult, InterviewPrepResult shapes)
    - src/components/ui/tabs.tsx (Tabs, TabsList, TabsTrigger, TabsContent API)
    - src/components/ui/button.tsx (Button variants)
    - src/components/auth-header.tsx (Plan 07-01 output)
    - src/components/status-select.tsx (Plan 07-01 output)
    - src/components/score-card-display.tsx, action-list-display.tsx, keyword-badges-display.tsx, rewrite-diff-readonly.tsx, question-card-display.tsx (Plan 07-03 output)
    - .planning/phases/07-history-ui/07-UI-SPEC.md (Detail Page Layout Contract + Copywriting Contract: "← Back to History", "Re-run Analysis", "Analysis", "Interview Prep", "Interview prep wasn't run for this application.", "Go back to run interview prep →")
    - .planning/phases/07-history-ui/07-PATTERNS.md (history/[id]/page.tsx section + Tabs default value + section heading typography)
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/page.md (Server Component dynamic params Promise unwrap; per AGENTS.md)
  </read_first>
  <action>
    Create `src/app/history/[id]/page.tsx` as a Next.js 16 Server Component (no `'use client'`; default async export).

    Imports:
    - `import { verifySession, getApplicationById } from '@/lib/dal'`
    - `import { notFound } from 'next/navigation'`
    - `import Link from 'next/link'`
    - `import { Tabs, TabsContent, TabsList, TabsTrigger } from '@/components/ui/tabs'`
    - `import { Badge } from '@/components/ui/badge'`
    - `import { AuthHeader } from '@/components/auth-header'`
    - `import { StatusSelect } from '@/components/status-select'`
    - `import { ScoreCardDisplay } from '@/components/score-card-display'`
    - `import { ActionListDisplay } from '@/components/action-list-display'`
    - `import { KeywordBadgesDisplay } from '@/components/keyword-badges-display'`
    - `import { RewriteDiffReadOnly } from '@/components/rewrite-diff-readonly'`
    - `import { QuestionCardDisplay } from '@/components/question-card-display'`
    - `import type { ApplicationStatus } from '@/lib/db/schema'`
    - `import { ReRunButton } from '@/components/re-run-button'` — wait, no: per UI-SPEC the Re-run button only navigates (no client logic beyond `router.push`). Implement it directly inline using `<Link href={`/?applicationId=${app.id}`} className="..."><Button>Re-run Analysis</Button></Link>`. Next.js `<Link>` triggers client-side navigation; no need for a separate router.push wrapper component.
    - `import { Button } from '@/components/ui/button'` (used for the Re-run button styling).

    Top of file (module-level constants, repeat from list page — do not import a shared map; UI-SPEC locks these per page):
    - `const STATUS_VARIANT: Record<ApplicationStatus, 'default' | 'secondary' | 'outline' | 'destructive'> = { saved: 'default', applied: 'secondary', interviewing: 'secondary', offer: 'default', rejected: 'destructive' }`
    - `const STATUS_LABELS: Record<ApplicationStatus, string> = { saved: 'Saved', applied: 'Applied', interviewing: 'Interviewing', offer: 'Offer', rejected: 'Rejected' }`

    `export default async function DetailPage({ params }: { params: Promise<{ id: string }> })`:
    1. `const { id } = await params` — Next.js 16 params unwrap.
    2. `const { userId, user } = await verifySession()` — redirects internally on no session.
    3. `const app = await getApplicationById(userId, id); if (!app) notFound()`.
    4. Render fragment:
       - `<AuthHeader userEmail={user.email} />`
       - `<main className="max-w-3xl mx-auto px-6 py-8">` (max-w-3xl per UI-SPEC — narrower than list).
       - Back link: `<Link href="/history" className="text-sm text-muted-foreground hover:text-foreground inline-block mb-6">← Back to History</Link>` (literal copy from UI-SPEC).
       - Header row: a flex container with the title block on the left and the action cluster on the right:
         ```
         <div className="flex items-start justify-between gap-4 mb-8">
           <div className="min-w-0">
             <h1 className="text-2xl font-semibold tracking-tight text-foreground truncate">{app.company}</h1>
             <p className="text-base text-muted-foreground truncate mt-1">{app.jobTitle}</p>
             <div className="mt-3 flex items-center gap-3">
               <Badge variant={STATUS_VARIANT[app.status]}>{STATUS_LABELS[app.status]}</Badge>
               <StatusSelect id={app.id} initialStatus={app.status} />
             </div>
           </div>
           <Link href={`/?applicationId=${app.id}`}>
             <Button size="lg">Re-run Analysis</Button>
           </Link>
         </div>
         ```
       - Tabs:
         ```
         <Tabs defaultValue="analysis">
           <TabsList>
             <TabsTrigger value="analysis">Analysis</TabsTrigger>
             <TabsTrigger value="interview">Interview Prep</TabsTrigger>
           </TabsList>
           <TabsContent value="analysis" className="space-y-10 mt-6">
             {app.analysisData ? (
               <>
                 <ScoreCardDisplay overallScore={app.analysisData.overallScore} components={app.analysisData.components} />
                 <ActionListDisplay items={app.analysisData.actionItems} />
                 <KeywordBadgesDisplay keywords={app.analysisData.keywordGaps} />
                 {app.analysisData.rewrites.length > 0 && (
                   <section aria-label="Accepted rewrites" className="space-y-4">
                     <h2 className="text-xl font-semibold tracking-tight text-foreground">Rewrites</h2>
                     {app.analysisData.rewrites.map((section, idx) => (
                       <RewriteDiffReadOnly key={`${section.sectionName}-${idx}`} section={section} />
                     ))}
                   </section>
                 )}
               </>
             ) : (
               <p className="text-sm text-muted-foreground">No analysis data saved for this application.</p>
             )}
           </TabsContent>
           <TabsContent value="interview" className="space-y-3 mt-6">
             {app.interviewData ? (
               app.interviewData.questions.map((q, i) => (
                 <QuestionCardDisplay key={i} question={q} index={i} />
               ))
             ) : (
               <div className="rounded-2xl ring-1 ring-border bg-card p-10 text-center">
                 <p className="text-sm text-muted-foreground mb-3">Interview prep wasn't run for this application.</p>
                 <Link href="/" className="text-sm text-foreground hover:underline">Go back to run interview prep →</Link>
               </div>
             )}
           </TabsContent>
         </Tabs>
         ```
       - Close `</main>` and fragment.

    Important rendering rules:
    - The persisted `rewrites` JSONB blob from Phase 6 only stores accepted rewrites (per the existing `saveApplication` flow), so no client-side filter on `status` is needed here. Map all entries directly into RewriteDiffReadOnly. (If a future audit shows pending rewrites leaked into the JSONB, add a filter — but per current Phase 6 contract, they don't.)
    - Do NOT import `AnalysisPanel` or `InterviewPrepPanel`. Those mount SSE on render (verified in 07-RESEARCH.md anti-patterns).
    - Empty-tab copy strings are LOCKED — "Interview prep wasn't run for this application." and "Go back to run interview prep →" must match UI-SPEC exactly.
  </action>
  <verify>
    <automated>test -f 'src/app/history/[id]/page.tsx' && grep -q "export default async function DetailPage" 'src/app/history/[id]/page.tsx' && grep -q "params: Promise" 'src/app/history/[id]/page.tsx' && grep -q "notFound" 'src/app/history/[id]/page.tsx' && grep -q "ScoreCardDisplay" 'src/app/history/[id]/page.tsx' && grep -q "ActionListDisplay" 'src/app/history/[id]/page.tsx' && grep -q "KeywordBadgesDisplay" 'src/app/history/[id]/page.tsx' && grep -q "RewriteDiffReadOnly" 'src/app/history/[id]/page.tsx' && grep -q "QuestionCardDisplay" 'src/app/history/[id]/page.tsx' && grep -q "Re-run Analysis" 'src/app/history/[id]/page.tsx' && grep -q "← Back to History" 'src/app/history/[id]/page.tsx' && grep -q "Interview prep wasn't run" 'src/app/history/[id]/page.tsx' && grep -vq "AnalysisPanel\\|InterviewPrepPanel" 'src/app/history/[id]/page.tsx' && grep -q "?applicationId=" 'src/app/history/[id]/page.tsx' && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `src/app/history/[id]/page.tsx` exists; default export is async; awaits both `params` and `verifySession()`.
    - Calls `notFound()` immediately when `getApplicationById` returns null.
    - Contains all five display component imports (ScoreCardDisplay, ActionListDisplay, KeywordBadgesDisplay, RewriteDiffReadOnly, QuestionCardDisplay) and the AuthHeader + StatusSelect imports.
    - Does NOT import `AnalysisPanel` or `InterviewPrepPanel` (verified by `grep -E "AnalysisPanel|InterviewPrepPanel" 'src/app/history/[id]/page.tsx'` returning empty).
    - Contains the literal strings `"Re-run Analysis"`, `"← Back to History"`, `"Analysis"`, `"Interview Prep"`, `"Interview prep wasn't run for this application."`, and `"Go back to run interview prep →"`.
    - Re-run is a `<Link href={`/?applicationId=${app.id}`}>` wrapping a `<Button>` — uses Next.js client-side navigation, no router.push wrapper.
    - `npx tsc --noEmit` exits 0.
    - Behavioural:
      - Visiting `/history/{owned-uuid}` while signed in renders the page with company/title heading, status select reflecting current status, Re-run button, Analysis tab populated when analysisData is non-null.
      - Switching to Interview Prep tab shows QuestionCardDisplay for each saved question when interviewData is non-null; shows the locked empty state otherwise.
      - Visiting `/history/{random-uuid}` (not in DB) returns Next.js 404.
      - Visiting `/history/{another-users-uuid}` returns Next.js 404 (DAL returns null, notFound() fires).
      - Visiting `/history/{owned-uuid}` while signed out redirects to `/sign-in` (proxy.ts guard).
  </acceptance_criteria>
  <done>Detail page renders saved data read-only with locked copy, Tabs split Analysis / Interview Prep correctly, Re-run navigates to the home page with the applicationId query param, and no SSE hooks fire.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Re-run support into the home page via searchParams + use() + prefill fetch + initialValue props</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx (current implementation — 'use client', useState for resumeText/jdText/analysing/showInterviewPrep/savedApplicationId)
    - src/components/resume-panel.tsx (now accepts initialValue — Task 1 output)
    - src/components/jd-panel.tsx (now accepts initialValue — Task 1 output)
    - .planning/phases/07-history-ui/07-RESEARCH.md (Pattern 3 — searchParams as Promise; Pattern 6 — prefill route; Open Question #1 on initialValue prop)
    - .planning/phases/07-history-ui/07-PATTERNS.md (src/app/page.tsx modification section)
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/page.md (Client Component page receives searchParams as Promise; must use React `use()` hook; per AGENTS.md)
  </read_first>
  <action>
    Modify `src/app/page.tsx` with surgical changes only — preserve all existing state, JSX, and panel wiring.

    Change 1 — Update the React imports line. Currently `import { useState } from 'react'`. Change to:
    `import { use, useState, useEffect } from 'react'`

    Change 2 — Update the default export's function signature to accept `searchParams`. Currently `export default function HomePage()`. Change to:
    ```
    export default function HomePage({
      searchParams,
    }: {
      searchParams: Promise<{ applicationId?: string }>
    }) {
      const { applicationId } = use(searchParams)
      // ... existing useState declarations remain unchanged below
    ```

    Change 3 — Add a `useEffect` immediately after the existing `useState` declarations and before `const bothReady = ...`:
    ```
    useEffect(() => {
      if (!applicationId) return
      fetch(`/api/applications/${applicationId}/prefill`)
        .then((r) => (r.ok ? r.json() : Promise.reject(r.status)))
        .then(({ resumeText, jdText }: { resumeText: string; jdText: string }) => {
          setResumeText(resumeText)
          setJdText(jdText)
        })
        .catch(() => {
          // Silent failure — user can still type manually. Do not block the page.
        })
    }, [applicationId])
    ```

    Change 4 — Pass `initialValue` through to both panels. Locate the existing `<ResumePanel onReady={setResumeText} locked={analysing} />` and change to `<ResumePanel onReady={setResumeText} locked={analysing} initialValue={resumeText} />`. Do the same for `<JobDescriptionPanel onReady={setJdText} locked={analysing} />` → add `initialValue={jdText}`.

    Wait — this creates a feedback loop: page sets `resumeText` from fetch, passes it as `initialValue`, panel's useEffect sets pasteText from initialValue and calls `onReady(initialValue)` which sets `resumeText` again (same value, stable). Since the panel useEffect depends only on `initialValue` (not on the setter or onReady reference), this stabilises after one cycle. Verify mentally: page sets resumeText=X → re-render → panel receives initialValue=X → panel's effect fires (X changed from undefined to X) → calls setPasteText/setExtractedText/onReady(X) → onReady === setResumeText, so setResumeText(X) → React bails out of re-render (Object.is(X, X) === true). Safe.

    Do NOT remove any existing state, do NOT change `setAnalysing(true)` button logic, do NOT remove `onSaved` / `onInterviewPrepReady` wiring.
  </action>
  <verify>
    <automated>grep -q "use, useState, useEffect" src/app/page.tsx && grep -q "searchParams: Promise" src/app/page.tsx && grep -q "use(searchParams)" src/app/page.tsx && grep -q "fetch(\`/api/applications/\\${applicationId}/prefill\`)" src/app/page.tsx && grep -q "initialValue={resumeText}" src/app/page.tsx && grep -q "initialValue={jdText}" src/app/page.tsx && npx tsc --noEmit && npx next build 2>&1 | tail -20 | grep -v "^$" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/page.tsx` imports `use`, `useState`, and `useEffect` from `react` on a single line.
    - HomePage signature accepts `{ searchParams }: { searchParams: Promise<{ applicationId?: string }> }`.
    - The line `const { applicationId } = use(searchParams)` exists.
    - A `useEffect` keyed on `[applicationId]` fetches `/api/applications/${applicationId}/prefill` and on success calls both `setResumeText` and `setJdText`.
    - `ResumePanel` is rendered with `initialValue={resumeText}` and `JobDescriptionPanel` with `initialValue={jdText}`.
    - No existing state or JSX outside the additive changes is removed (verified by re-reading the file before/after and confirming `analysing`, `showInterviewPrep`, `savedApplicationId`, `onSaved`, `onAnalysisStart`, `onInterviewPrepReady` still appear).
    - `npx tsc --noEmit` exits 0.
    - `npx next build` succeeds (no Next.js 16 deprecation errors, no missing-promise-unwrap errors).
    - Behavioural:
      - Visiting `/` (no query param) renders the home page exactly as before — Resume panel and JD panel start empty, "Analyse match" button hidden until both are filled.
      - Visiting `/?applicationId={owned-uuid}` triggers a prefill fetch; within ~1 second the Resume panel shows the saved resume text (paste tab selected, textarea pre-filled, TextPreview visible) and the JD panel shows the saved JD text. The "Analyse match" button appears immediately because both states are non-empty.
      - Visiting `/?applicationId={non-owned-uuid}` silently fails — page stays empty, no error banner (acceptable per simplicity rule; the user can still type).
  </acceptance_criteria>
  <done>Home page accepts searchParams as a Promise (Next.js 16-compliant), fetches the prefill payload when applicationId is present, and threads the saved resume/JD into both panels via the new initialValue prop. Existing analysis flow is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /history/[id] (server page) | Untrusted user session cookie + dynamic path param cross into server-rendered page |
| browser → GET /api/applications/[id]/prefill | Untrusted session + path param cross into route handler that returns resume + JD text |
| browser → /?applicationId={id} (client page) | Untrusted searchParam used to trigger a prefill fetch — IDOR enforced by the prefill route |
| client (home page) → server (prefill route) | Fetch propagates the user's session cookie automatically; no client-side userId sent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-15 | Information Disclosure | /history/[id] page | mitigate | verifySession() first, then getApplicationById(userId, id) — DAL WHERE clause `eq(applications.userId, userId)` blocks cross-tenant reads. notFound() called when row is null, so a user probing another user's IDs sees a generic 404, not a "forbidden" leak. |
| T-07-16 | Information Disclosure | /api/applications/[id]/prefill | mitigate | Same verifySession + getApplicationById guard as the detail page. Response body is restricted to `{ resumeText, jdText }` — analysisData, interviewData, jobTitle, company, status, matchScore are NOT included (verified by acceptance grep). |
| T-07-17 | Elevation of Privilege | /history/[id] page (IDOR) | mitigate | userId comes exclusively from the session, never from the URL or query. getApplicationById ignores ID mismatches at the WHERE level. notFound() returns the same 404 for "row missing" and "row not owned by you" — no oracle for ID guessing. |
| T-07-18 | Tampering | searchParam injection (?applicationId=) | mitigate | applicationId is interpolated into a URL fragment that is fetched by the same browser using its own session cookie. If the user manipulates the param to another user's ID, the prefill route returns 404 and the home page silently stays empty. No XSS vector — applicationId is never rendered as HTML, only interpolated into a fetch URL. |
| T-07-19 | Tampering | XSS via persisted analysis/interview JSONB | mitigate | All rendered fields (overall score, components, action items, keyword strings, rewrite sectionName/originalText/rewrittenText, question text, rationale, modelAnswer) flow through React JSX text interpolation; no `dangerouslySetInnerHTML`. InlineDiff (existing component) is also XSS-safe per earlier verification. |
| T-07-20 | Tampering | Re-run feedback loop (initialValue → onReady → resumeText → initialValue) | mitigate | The panel's `useEffect` only fires when `initialValue` reference identity changes. After the first fire, setResumeText/setJdText receive the same string, React bails out of re-rendering via Object.is, and the effect does not re-trigger. Verified by reasoning + acceptance test "no infinite re-render in browser DevTools". |
| T-07-21 | Spoofing | Re-run navigation produces a new application record under a different user | accept | Re-run flow is intentionally non-destructive: it copies resume+JD text into the form and the user re-runs analysis, which saves a NEW row via Phase 6 saveApplication(userId) using the current session's userId. If user A signs out and user B signs in mid-flow, user B owns the new record — this is by design (the re-run reuses text, not identity). |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | Zero package installs in this plan. Package Legitimacy Audit in 07-RESEARCH.md confirms no new runtime deps. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 across all three tasks.
- `npx next build` succeeds with no Next.js 16 deprecation errors (specifically: no "params should be awaited", no "searchParams should be a Promise" warnings).
- `/history/[id]` for a valid owned ID renders all read-only sections; for an invalid or non-owned ID returns 404.
- `/?applicationId={owned-id}` populates both input panels and enables the "Analyse match" button.
- GET `/api/applications/[id]/prefill`: 200 owner JSON `{ resumeText, jdText }` only, 401 unauthenticated, 404 not-owner.
- Re-run button on detail page navigates to `/?applicationId=ID` (DevTools URL check).
- Status select on detail page persists per Plan 07-02's PATCH route; revert behaviour on failure preserved.
- No console errors in browser DevTools when navigating /history → /history/[id] → /?applicationId=ID flow.
</verification>

<success_criteria>
- HIST-03: User opens /history/[id] and sees full read-only analysis (score, action items, keyword gaps, accepted rewrites). Verified.
- HIST-04: Interview Prep tab shows QuestionCardDisplay list when interviewData exists, locked empty state otherwise. Verified.
- HIST-05: Re-run Analysis button navigates to /?applicationId=ID, home page fetches prefill, both panels populate, user can hit Analyse match. Verified.
- All five HIST requirements (HIST-01 through HIST-05) are covered across the four phase plans.
</success_criteria>

<output>
Create `.planning/phases/07-history-ui/07-04-SUMMARY.md` when done — list files added/modified, the curl + browser-flow transcripts proving each of the three tasks works end-to-end, and confirm that Phase 7's five success criteria (per ROADMAP.md) are all satisfied.
</output>
