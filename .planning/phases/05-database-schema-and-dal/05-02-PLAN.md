---
phase: 05-database-schema-and-dal
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - drizzle/
autonomous: false
requirements: [DATA-01, DATA-02]
user_setup:
  - service: neon
    why: "Postgres database for persisting applications and better-auth tables"
    env_vars:
      - name: DATABASE_URL
        source: "Neon Dashboard → Project → Connection Details → Pooled connection string"
      - name: DATABASE_URL_UNPOOLED
        source: "Neon Dashboard → Project → Connection Details → Direct connection string (toggle 'Pooled connection' OFF)"
    dashboard_config:
      - task: "Create a Neon Postgres project"
        location: "console.neon.tech → New Project"
must_haves:
  truths:
    - "drizzle-kit generate produces SQL migration files in ./drizzle (DATA-01)"
    - "drizzle-kit migrate applies the schema to the live Neon DB with no errors (DATA-01)"
    - "The applications table and application_status enum exist in the live database (DATA-02)"
    - "better-auth user/session/account/verification tables exist in the live database (DATA-02)"
  artifacts:
    - path: "drizzle"
      provides: "Committed SQL migration files"
      contains: "CREATE TABLE"
  key_links:
    - from: "drizzle.config.ts"
      to: "Neon database (DATABASE_URL_UNPOOLED)"
      via: "drizzle-kit migrate"
      pattern: "drizzle-kit migrate"
---

<objective>
Provision the Neon Postgres database, generate the SQL migration from the Plan 01 schema, and apply it to the live database.

Purpose: Without a migrated live database, no DAL function can run and the auth adapter swap (Plan 03) would break sign-in at runtime. This plan turns the schema source code into real tables.
Output: Generated and committed SQL migration files in `./drizzle`; a live, migrated Neon database.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-database-schema-and-dal/05-CONTEXT.md
@.planning/phases/05-database-schema-and-dal/05-RESEARCH.md
@.planning/phases/05-database-schema-and-dal/05-PATTERNS.md

<interfaces>
<!-- This plan consumes artifacts from Plan 01. They must exist before starting. -->
- drizzle.config.ts (root) — out: './drizzle', schema: './src/lib/db/schema.ts', dbCredentials.url = DATABASE_URL_UNPOOLED
- src/lib/db/schema.ts — applications table + better-auth tables (the migration source)

Migration commands (RESEARCH.md "Migration workflow commands"):
  npx drizzle-kit generate   # diffs schema.ts → SQL in ./drizzle
  npx drizzle-kit migrate    # applies SQL using DATABASE_URL_UNPOOLED
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Provision Neon and add credentials to .env.local</name>
  <files>.env.local</files>
  <action>Human-action checkpoint — no Claude-authored code. The user provisions a Neon project and pastes both connection strings into .env.local; the names must match the placeholders added to .env.example in Plan 01 (DATABASE_URL pooled, DATABASE_URL_UNPOOLED direct). Exact steps are in what-built / how-to-verify below.</action>
  <what-built>Plan 01 created drizzle.config.ts which reads DATABASE_URL_UNPOOLED, and src/lib/db/index.ts which reads DATABASE_URL. Neither variable has a value yet, and no Neon project exists. These cannot be created by CLI/API without the user's Neon account — this is a genuine human step (RESEARCH.md Open Question 1, Environment Availability table).</what-built>
  <how-to-verify>
    1. Go to console.neon.tech and create a new Postgres project (any region; Postgres 16+).
    2. Open Connection Details for the project.
    3. Copy the POOLED connection string into `.env.local` as `DATABASE_URL=...` (this is the default "Pooled connection" string).
    4. Toggle "Pooled connection" OFF (or pick "Direct connection") and copy that string into `.env.local` as `DATABASE_URL_UNPOOLED=...`.
    5. Confirm `.env.local` is gitignored (it must NOT be committed). `.env.example` already documents both variables.
  </how-to-verify>
  <resume-signal>Type "provisioned" once both DATABASE_URL and DATABASE_URL_UNPOOLED are present in .env.local with real Neon values.</resume-signal>
  <read_first>
    - .env.example (the two placeholder entries added in Plan 01 — mirror their names exactly in .env.local)
    - .planning/phases/05-database-schema-and-dal/05-RESEARCH.md (Pitfall 1 pooled-vs-unpooled; Open Question 1; Environment Availability)
  </read_first>
  <verify>
    <automated>git check-ignore .env.local && grep -q "^DATABASE_URL=." .env.local && grep -q "^DATABASE_URL_UNPOOLED=." .env.local && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.env.local` contains a non-empty `DATABASE_URL=` value (pooled)
    - `.env.local` contains a non-empty `DATABASE_URL_UNPOOLED=` value (direct/unpooled)
    - `.env.local` is not tracked by git (`git check-ignore .env.local` exits 0)
  </acceptance_criteria>
  <done>Both Neon connection strings are present in .env.local; the Neon project is live.</done>
</task>

<task type="auto">
  <name>Task 2: Generate and apply the migration</name>
  <files>drizzle/</files>
  <read_first>
    - drizzle.config.ts (confirm out/schema/dbCredentials before running)
    - src/lib/db/schema.ts (the schema being migrated)
    - .planning/phases/05-database-schema-and-dal/05-RESEARCH.md ("Migration workflow commands"; Pitfall 6 pgEnum mismatch; "Primary recommendation": generate-then-migrate, not push)
  </read_first>
  <action>
    Run `npx drizzle-kit generate` to diff `src/lib/db/schema.ts` against the (empty) database and produce SQL migration files in `./drizzle`. Inspect the generated SQL to confirm it creates the `application_status` enum, the `applications` table (with all D-03 columns), and the four better-auth tables. Then run `npx drizzle-kit migrate` to apply the migration — this uses `DATABASE_URL_UNPOOLED` from drizzle.config.ts. Do NOT use `drizzle-kit push` (no committed migration files, not rollback-safe — RESEARCH.md anti-pattern). If migrate hangs or returns "prepared statement already exists", the config is pointing at the pooled URL — verify drizzle.config.ts uses `DATABASE_URL_UNPOOLED` (Pitfall 1). Commit the `./drizzle` directory to source control.
  </action>
  <verify>
    <automated>test -d drizzle && ls drizzle/*.sql >/dev/null 2>&1 && grep -rqi "create table" drizzle/ && grep -rqi "applications" drizzle/ && grep -rqi "application_status" drizzle/ && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `./drizzle` directory contains at least one `.sql` migration file
    - The generated SQL contains `CREATE TABLE` for `applications` and the better-auth tables
    - The generated SQL creates the `application_status` enum type
    - `npx drizzle-kit migrate` completed without error (no hang, no enum/prepared-statement errors)
    - The `./drizzle` directory is committed
  </acceptance_criteria>
  <done>The schema is applied to the live Neon database; migration SQL is committed for reproducibility.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm tables exist in the live database</name>
  <files>drizzle/</files>
  <action>Human-verify checkpoint — no Claude-authored code. The user inspects the live Neon database to confirm the migration from Task 2 created the expected tables, enum values, and text columns. Exact queries are in how-to-verify below.</action>
  <what-built>Task 2 ran drizzle-kit migrate against the live Neon DB, creating the applications table, the application_status enum, and the better-auth tables. This checkpoint confirms the success criteria #1 from ROADMAP Phase 5: "Running drizzle-kit migrate against DATABASE_URL_UNPOOLED applies the schema with no errors."</what-built>
  <how-to-verify>
    1. In the Neon dashboard SQL editor (or `psql "$DATABASE_URL_UNPOOLED"`), run: `\dt` (or `SELECT tablename FROM pg_tables WHERE schemaname='public';`).
    2. Confirm the table list includes `applications`, `user`, `session`, `account`, and `verification`.
    3. Run `SELECT enumlabel FROM pg_enum JOIN pg_type ON pg_type.oid = pg_enum.enumtypid WHERE typname='application_status';` and confirm the five values: saved, applied, interviewing, offer, rejected.
    4. Run `\d applications` and confirm columns include resume_text/resumeText and jd_text/jdText (column name casing depends on Drizzle config) — DATA-03.
  </how-to-verify>
  <resume-signal>Type "verified" if all tables, the enum's five values, and the resume/jd text columns are present; otherwise describe what is missing.</resume-signal>
  <read_first>
    - .planning/ROADMAP.md (Phase 5 Success Criteria #1, #3, #4)
  </read_first>
  <verify>
    <human-check>Live DB lists applications + user/session/account/verification tables; application_status enum has 5 values; applications has resume/jd text columns.</human-check>
  </verify>
  <acceptance_criteria>
    - Live DB lists tables: applications, user, session, account, verification
    - application_status enum has exactly five values: saved, applied, interviewing, offer, rejected
    - applications table has resume text and jd text columns present
  </acceptance_criteria>
  <done>Human confirms the migrated schema matches the design in the live database.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration runner → live DB | drizzle-kit migrate executes DDL against production data store |
| .env.local → git | Real connection strings must stay out of source control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Information Disclosure | .env.local | mitigate | Credentials live only in gitignored .env.local; checkpoint verifies `git check-ignore .env.local` exits 0 |
| T-05-04 | Tampering | drizzle-kit migrate on pooled URL | mitigate | Config pinned to DATABASE_URL_UNPOOLED (Pitfall 1); committed SQL migration files make DDL reviewable before/after apply |
| T-05-SC | Tampering | npm/npx execution | accept | Runs drizzle-kit (already installed, audited [OK] in RESEARCH.md); no new package installs |
</threat_model>

<verification>
- `ls drizzle/*.sql` returns at least one file.
- Generated SQL contains `CREATE TABLE` for applications and the enum type.
- `npx drizzle-kit migrate` exits 0.
- Human checkpoint confirms tables and enum exist in the live Neon DB.
</verification>

<success_criteria>
- Neon project provisioned; both connection strings in .env.local (gitignored).
- Migration SQL generated and committed in ./drizzle.
- Schema applied to the live database with no errors (ROADMAP Success Criteria #1).
- applications table, application_status enum (5 values), and resume/jd text columns confirmed present (Success Criteria #3, #4).
</success_criteria>

<output>
Create `.planning/phases/05-database-schema-and-dal/05-02-SUMMARY.md` when done.
</output>
